Описание текущих версий и планов на будущее

Текущая версия: *0.2*

**Alpha стадия**
-

*v 0.1*
```diff
front-end:
+ Поддержка команды в чате. Команды начианются с символа /
+ поддержка команды /password (/p) для смены текущего пароля. После команды вводится парольная фраза
+ поддержка команды /exit (/e) - остановка обмена и выход из программы (вызов stop() клиента)
+ реализовать вывод текущей версии клиента /version (/v)

back-end:
+ реализация функции set_password для клиента
+ реализовать функцию stop() для клиента для корректного выхода из потоков
+ реализовать команду stop() для сервера для корректной остановки сервера 

tests:
+ реализовать тест отправки и получения сообщения
+ реализовать тест выполнения команды в меню
```

*v 0.2*
```diff
front-end:
+ поддержка команы /disconnect (/d) для разрыва соединения
+ поддержка команды /connect (/c) для установки соединения. После команды вводится адрес сервера в виде 1.2.3.4:9191. Коннект без параметров означает подключение к предыдущему серверу.
+ сохранение данных о настроек клиента в файл при остановке клиена и загрузка призапуске.

back-end:
+ реализация пересоединение к серверу
+ реализовать end-to-end шифрование отправляемых сообщений симметричным ключем

tests:
+ реализовать тест переподключения клиента
- дополнить тествыполнения команды в меню
```

*v 0.3*
```diff
front-end:
+ поддержка команды /server chroom (/r) для перехода в комнату. Полсе команды вводится название комнаты.
+ поддержка команды /server rooms для вывода всех комнат.
+ "форма" регистрации на сервере

back-end:
+ реаизовать регистрацию на сервере и клиенте
+ реализовать поддержку команат на сервере. Комната создаётся администратором сервера, и хранится в конфиге. Сообщения направленные в комнату уходят только в нее.

tests:
- реализовать тест отправки сообщений по комнатам
+ реализовать тесты работы с базой данных
```

*v 0.4*

```diff
front-end:
- поддержка команды /server users для вывода всех пользователей на сервере.
- поддержка команды /server inroom для вывода всех участников комнаты
- поддержка команды /server chat __username__ __message__ для чата друг с другом.

back-end:
- реализовать поддержку ассимметричного шифрования
- реализовать стэк сообщений, которые были отправлены пользователю, который был не в сети.

tests:
- реализовать тест ассимметричного шифрования
- руализовать тест новых команд
```

**Beta стадия**
-

*v 1.0*

```diff
front-end:
- Реализовать графический интерфейс для клиента
- Поддержка drag-and-drop в клиенте

back-end:
- реализовать поддержку отпавки аудио/видео/фото контента
- реализовать поддержку команат на сервере. Комната создаётся администратором сервера, и хранится в конфиге. Сообщения направленные в комнату уходят только в нее.

tests:
- реализовать тест отправки медиаконтента
```

*v 1.1*

```diff
front-end:
- Поддержка отображения медиаконтента
- Поддержка drag-and-drop в клиенте

back-end:
- реализовать кэш клиента и сервера, для хранения медиафайлов (Сервер хранит в зашифрованном виде.)
- реализовать поддержку потокового аудио\видео

tests:
- реализовать тест потокового видео
- реализовать тест потокового аудио
- реализовать тесты кеширования
```